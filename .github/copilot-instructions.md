# Project conventions

## Git workflow

**Never run `git push` (or any command that pushes to a remote) unless
I explicitly say the word "push" in my message.** This rule is
absolute. If a task seems to require pushing, stop and ask me first
rather than pushing on your own.

You are otherwise free to commit, branch, rebase, etc. locally.
